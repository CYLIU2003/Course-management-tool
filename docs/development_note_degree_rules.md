# 区分・必要単位・必選欄の原本照合（2026-09-07、作業中）

この変更はローカル検証中。全学科・大学院の全履修条件を確定したリリースではなく、本番・GitHubへの反映も未実施。

## 保存した情報

- 学部2022～2026年度の94学科・入学年度について、各年度の学則第15条の原本16ページを画像で確認。情報工学部の一般・国際コースを分けた104組、530区分の最低単位を `data/verified/undergraduate_degree_rules.json` に記録した。2022年度のデザイン・データ科学部は開設前で対象外。
- SQLiteの `degree_requirement_sets` と `degree_category_rules` に入学年度・コース・最低単位・元PDFのハッシュとページを保存。再取込でルールの欠落、別年度への混入、出典変更があれば停止する。
- 自由選択は区分の最低単位を超えた修得単位の合計として扱い、最低単位と二重加算しない。履修予定・算入対象外科目は加算しない。算入上限を含めた卒業判定そのものは未完了。
- 座標照合済み科目行の区分階層を、元PDFの結合セルから再取得。アウトライン文字の線が表の罫線と誤認される問題、縦書きの列順、ページをまたぐ親区分を修正。目視修正は `classification_regions.json` に原本ハッシュ・領域を記録。
- 必選欄の○・△・番号を、科目名と同じ行の必選ヘッダーに属するセルから取得。資格欄・教育方法欄の記号は必選欄に使わない。複数列は別々に保存し、一律に必修とはしない。
- SQLiteの `verified_course_categories` と `verified_course_requirements` ビューから、確認済み区分階層・必選列・対象学科・出典ページをSQLで参照できる。元JSONと別の複製を更新する方式を避け、ハッシュ一致・座標確認済みの行だけを公開する。
- 1ページに複数の教育課程表がある場合、それぞれの必選ヘッダーを読むよう修正。線だけの表抽出でヘッダーを失うPDFは、元の描画を含む表から再取得する。
- 2022理工学基礎科目の見開きPDF10～11ページは、右ページの科目番号・行位置・8学科／コース列を照合。例えば10-211は機械工学科で○、電気電子通信工学科で△1。出典を限定して対応し、任意の隣接ページを結合しない。
- 7ページの科目名を追加目視し、Next PBLの先頭欠落2行、科目群の見出しが科目名に混ざった8行を特定。正しい別行を残して誤抽出行を隔離し、対応は `title_reviews.json` に保存。原本PDF・元の抽出行を削除しない。

## フロント

履修ガイドはSQLite APIの学科・入学年度データに含まれる必要単位を読む。画面内でPDFの表を検索して最低単位を推定する経路を外した。年度切替時は取得を中断し、古い年度のルールを表示しない。

区分カードに最低単位、修得単位、履修予定、残りの数値、該当科目と科目群を表示。必修・選択必修は出典付き必選セルを確認できた場合に表示する。情報工学部は所属コースの選択を求める。自由選択は独立した超過単位カードとした。

## 再現手順

既存PDF・抽出JSONを使う場合（PowerShellでは `npm.cmd`）：

```powershell
python scripts/curriculum/apply_title_reviews.py
python scripts/curriculum/classify_pdf_courses.py --unresolved-only
python scripts/curriculum/extract_requirement_cells.py
npm.cmd run db:build
node scripts/audit-guide.mjs
python -m unittest discover -s scripts/curriculum -p test_classification.py
python -m unittest backend.test_database backend.test_accounts backend.test_administration
npm.cmd run verify:cloudflare
```

原本更新時は既存ハッシュ付き修正を使い回さず、変更箇所を再確認する。`verify_pdf_courses.py` は科目名の目視修正も適用する。大学院の分類は `db:build` 最後の大学院インポーターで行う。

## 検証と限界

- 学部104組すべてのガイド描画で最低単位と区分の対応を検査する `test:degree-guide` を公開前検証に追加。
- 自由選択の区分別超過、予定の除外、国際コースの外国語12単位と一般8単位の分離をテスト。
- 原本の縦書き・隣接文字・罫線・必選並列列・見開きの学科列について回帰テスト。
- Chromeの390px幅と1366px幅で実コンポーネントを描画し画像確認。検証用データを使用し、実ユーザーの履修記録は変更していない。
- SQLite整合性検査は `ok`。個人状態・アカウント・管理機能のテストは通過。直近の再検証結果は作業終了時に追記する。

### 直近の再検証

- 学部95枠のうち科目データのある94枠すべてで区分未確認0行。
- SQLite: 182学科・課程／入学年度、104最低単位ルール、530最低単位区分。単位付き科目19,563行の分類、17,104行・21,948列の必選欄を確認済みビューで参照可能。単位制ではない共同原子力博士の研究活動29行は `reference_activities` に別保存しており、この科目件数に含めない。[集計結果](degree-rule-verification.json)。
- Python: 原本分類・必選・科目名の10テスト、大学院1テスト、DB・アカウント・管理24テスト通過。
- `test:degree-guide` の104組、`verify:cloudflare`、更新後の最終公開ビルド、変更TypeScriptのESLint通過。公開ビルドには575kBのJSチャンクに関する性能警告が残る。
- SQLite再構築完了、整合性 `ok`、外部キー違反0件。最終更新後の390px画面をChromeで再確認した。
- 上記の通過は全条件の認定完了や本番反映を意味しない。下記のP1と公開更新は未完了。

## Code Review Summary

Reviewer: Codex（自己レビュー）、対象: main `7e34116` からの作業差分。

原本ハッシュと座標、入学年度、コースを保持しており、表示だけで未確認を消す変更にはしていない。一方で以下のP1が残るため、要件全体についてLGTMとはしない。Claude Code・担当者による外部レビューは未実施。

- P0 [BLOCKER]: 今回の確認範囲で未解決の検出なし。アプリ全体の無欠陥を意味しない。
- P1 [MUST]: 必選欄の空欄・網掛け・備考から対象学科と選択科目の資格を確定する処理が未完了。空欄を自動的に選択扱いにしない。
- P1 [MUST]: 選択必修△1等の各群最低単位、算入上限、コース別の例外条件の全件構造化が未完了。
- P1 [MUST]: 大学院の修了最低単位と例外条件はまだ学部と同じルールテーブルへ統合していない。
- P2 [SHOULD]: 情報工学部の所属コース選択をプロフィールとして永続保存する必要がある。
- P2 [SHOULD]: 全条件の確認後、Supabase参照データの更新、公開画面の検証、GitHub同期が必要。

次の作業は残るP1の原本照合と実装。科目名一致だけで必選区分・卒業算入を認定しない。


## 2026-09-07 追加照合（進行中）

- 人間科学部の2コース、都市生活学部2022–2024年度の2コースを分離し、学部111組に拡張。731件の必修・選択必修群を登録した。
- `degree_group_rules` ビューに群の最低単位、必選記号、適用区分、入学年度、コース、出典を公開。インポート時に出典SHAとページ、群ID重複を検証する。
- ガイドで群ごとの単位進捗と対象科目を表示する。必修は最低単位を満たしても未修得の必修科目が残れば達成としない。
- 全コースの科目群照合で、罫線の誤分割による必選列の欠落を検出。2025情報科学・原子力安全、2024自然科学、2023・2025都市生活の原本を画像で確認し、SHA付き列領域を追加した。全資料の再抽出と再照合を実行中。
- これらは作業途中の記録であり、群対応の全件合格、大学院修了条件の完成、本番反映を示さない。直前の104組の検証結果はその時点の結果として保持する。


### 抽出処理の再点検

- 授業形態の注記は元セルの改行で区切られた形式を根拠に除去する。科目名に含まれる「演習」を削らない。
- 科目番号・行位置・単位列によって同名の科目群と授業を区別する。回復時のタイトル位置は同じ単位列にある確認済み科目の位置を参照し、左側の科目群を授業名へ混入させない。
- 旧年度の数字2桁の科目番号にも対応。単位列と週時間列の連結による11単位等は、一意な原本単位列で検証して補正し、元の抽出値を残す。
- 2022–2026学部の位置確認済み18,836行について区分の対応が得られた。必選・指定群の再照合は別ゲートで継続する。
- 111組すべての原本からDS/MS計4単位の条件を登録。DS1単位の内数は当該ページに明記された場合に追加する。これらは科目区分の内数であり卒業単位に二重加算しない。
- 大学院の17件の年度別修了条件表を画像で照合し `data/verified/graduate_degree_minima.json` に保存。2025情報データ科学の表内訳と2024国際連携のページ間不一致は明示して保持し、訂正根拠を照会中。
- 環境情報・都市生活の研究科目表は「必修科目」ヘッダーと「計」列から直接読む。全5年度で各5領域・各6科目・各10単位（合計150行）の原本テストが通過。結合された単位セルも行位置で分離する。
- 公開前検証に `test:degree-membership` を追加。不一致を残したままデプロイしない。

### 2026-09-07 再抽出・保存処理の追加検証

- 必選の2列を1列と誤読する問題、複数学科共通ページを対象外にする問題、別表の単位欄を混同する問題を修正した。授業形態だけの「講義」「演習」は科目として採用しない。
- 111組・731群の照合で抽出起因の不一致0件を確認した時点の結果を `docs/degree-membership-audit.json` に保存。全資料を再生成して再現性を再検査中。
- 2025都市生活の原本63ページの○印合計20単位と64ページの必修23単位は一致しない。数値・ページ・SHAを `sourceConflicts` に保存し、この群の条件達成表示を停止する。これは抽出不具合の免除ではなく、両方の原本記載が変われば失敗する独立した検査である。2025情報データ科学、2024国際連携の原本不一致も継続記録する。
- 2026人間科学部のDS/MSは70ページの指定科目表から科目名と単位数の両方で照合。授業名だけからプログラム指定を推測しない。
- 所属コースを履修プロフィールに保存する。SQLite schema 5は既存プロフィールを保持してnullable列を追加。本番は追加migration `202609070002_degree_variant.sql` で学科・入学年度とvariantの一致を検証する。既存クライアントのvariantなし保存にも対応する。
- Supabaseの実PostgreSQL互換テストで、不正variant、数値型、競合保存、別アカウントへの漏れを検証して通過した。
- 大学院は修了条件の各区分に該当科目と修得済み単位を表示する。年度をまたぐ同一科目の重複登録は1回、履修予定は修得単位に含めない。単位制でない共同原子力博士課程は研究活動として表示する。
- 国際連携の単位セルに隣の大学名の文字が混入するPDF境界問題を修正し、開設大学の原文・座標を保管する。他専攻科目の許可条件と単位を伴わない活動も保持する。
- 本番反映・GitHub同期・最終全件検証はまだ実行していない。外部レビューも未実施。

## v0.3.0 ローカル検証・公開準備（2026-09-07）

### 確認できた範囲

- SQLite schema 5、整合性 `ok`。学部95・大学院87の計182データセット、原本184件。公開用参照APIは373件、102,934,466 bytesを出力し、importのdry-runが通過した。
- 学部の位置確認済み19,338行は区分対応の未解決0件。111課程・コース組の731群は抽出起因の対応不一致0件。2025都市生活の原本不一致1件は別項目で検証・保管している。
- 大学院87組について年度・課程・出典SHAと各区分の候補単位数を検証。国際連携の開設大学、0単位の活動、博士課程の非単位制を区別した。
- `verify:cloudflare` 全ステップ、公開用ビルド、Python 49テストが通過。追加のschema 4→5プロフィール保持テストを実施する。公開用Static Assetsは574件、最大9,433,717 bytesで制限内。
- SQLite由来の書き出しを使うChrome検証画面で、学部の群別表示・DS/MSと大学院の必要単位・科目展開を確認。390px幅の学部表示は横方向のはみ出し0。実利用者の成績は変更していない。
- 検証用ブラウザーが古いJSを使用していたため、別ファイル名で再配信して再確認した。これはローカル検証ハーネスの問題で、Vite本番ビルドはハッシュ付きファイル名を使用する。
- 起動スクリプトがschema 4・95件を固定判定していた不具合を修正。schema 5・学部大学院182件以上を要求し、ローカルAPIを再起動した。
- 時間割コピーの `any` を既存セル型に置き換え、Hookの依存配列と未使用引数を修正。`npm run lint` は現行アプリと生成スクリプト（`src scripts`）を対象として通過。リポジトリ直下の過去の単発パッチとignored `tmp` ハーネスは対象外。過去パッチの構文修復をしたという意味ではない。

### 更新手順

1. `npm.cmd run db:build`
2. `npm.cmd run db:offerings`（開講情報を更新する場合。年度データを保持する）
3. `python -m unittest discover -s scripts/curriculum -p "test_*.py" -v`
4. `python -m unittest backend.test_database backend.test_accounts backend.test_administration backend.test_offerings -v`
5. `npm.cmd run lint`、`npm.cmd run verify:cloudflare`
6. `python -m scripts.export_supabase`、`python -m scripts.import_supabase`（検査のみ）
7. Supabase参照データを更新し、追加migration `202609070002_degree_variant.sql` を適用する。
8. 公開用フロントをデプロイし、取得した本番参照データをローカルsnapshotと照合する。その後GitHub同期を確認する。

### 残る問題と公開状態

- 原本間の不一致：2025都市生活の必修20/23単位、2025情報データ科学の研究内訳、2024国際連携の本学研究3/4単位。訂正根拠がない数値は補わず、影響する条件を達成認定しない。
- 群最低単位・必選記号の対応は完了したが、他専攻科目の許可、算入上限、履修資格、研究指導・論文審査を含む全条件の自動卒業認定は未完了。単位数の進捗を卒業認定と同一視しない。
- Supabase Dashboard用ブラウザーツールが接続タイムアウト、Windows操作ツールが接続先不在となった。DB接続資格情報はこの環境にないため、接続の再開を依頼済み。5つのSHA付き公開データ束とSQLは `data/reference-release-v030/` に準備したが、まだ実行していない。
- 本番はこの作業のv0.3.0へ未更新。GitHub同期も未実施。変更前の本番に新フロントだけを先に配信しない。
- 外部レビュー（Claude Code・担当者）は未実施。上記の未完了条件を含む要件全体についてLGTMとはしない。

最終追記：schema 4→5の既存プロフィール保持テストを含むDB 11テストが通過。修正後の公開検証・ビルドを再実行して通過した。111組の全ガイドについて、区分最低単位の未確認表示に加え「必修・選択の条件は原本確認」のフォールバックが残らないこともテストで確認した。ローカルAPIはschema 5・182件を返し、`npm run dev -- --port 5180` の起動も確認済み。

## 2026-09-08 本番データ・フロント反映

- ユーザーが操作用ChromeでSupabaseへ認証し、`campus-note-prod`（`rnsqvyejfimzxwfkzfkf`）を確認。
- 更新前は参照373件、`campus_degree_variant` triggerなし、http拡張なし。プロフィール・保存状態は各2件。
- 現行HTML/JS/CSSを維持したbootstrap配信 `46e56f92-c551-475f-9273-8d6628d1818f` に公式データ5束を追加。SQL内でHTTPステータス・SHA・パス・件数を検証し、参照データと `202609070002_degree_variant.sql` を同一トランザクションで適用した。
- 更新後も参照373件、プロフィール・保存状態各2件。triggerあり、http拡張なし。参照データ以外の学生レコードを編集するSQLは実行していない。
- 本番RESTを5件ずつ取得し、ローカルSQLiteから出力した373 payloadとJSON全体を比較。欠落0・余分0・差分0。
- 公開前の全検証を再実行し、Cloudflareへv0.3.0をデプロイ。version ID: `4a361906-b57e-4f29-b7ef-bddaa2efe2b5`。公開HTMLとJS/CSSのバイト一致、一時データ束の撤去を確認した。
- 配信JS: `/assets/index-DjP-5zjg.js`、SHA-256 `a242a3485ae3dc2737827a307b0f549046cbdee91af33969de4e0a370d081cf4`。CSS: `/assets/index-BKrQzGmE.css`、SHA-256 `dd9dd749d5830d1f8e6d46032fbfc0a0b336de91d88282166e03280648fd1546`。
- GitHub mainはコードコミット `4dd3e5d` と一致していた。公開記録の追記も同期する。
- ログイン画面の表示は確認済み。ログイン後の本番履修ガイドはユーザーのGoogle認証待ちで、まだ実画面確認完了とはしない。ローカルでの111組レンダリング検証と本番全データ一致の検証とは区別する。

### 今後の調査・編集の完了条件

データ調査や編集の完了を本番反映と同一視しない。SQLite再生成・原本検証、Supabase同期、必要に応じたフロント公開、公開API照合、変更した操作画面の確認、README/開発ノートとGitHub同期までを一連の更新として扱う。認証待ち・原本不一致・画面未確認が残る場合は、どこまで反映したかと残る作業を明記する。
